1. run "pip install -r requirements.txt" or "pip install transformers torch"
2. create venv "python -m venv reformer"
2. turn on the enviroment "reformer\Scripts\activate"
4. run "python <scriptname.py>"